import useDebounce from "use-debounce/lib/useDebounce";

/**
 * useDebounce
 *
 * @param value
 * @param delay number
 * @param options maxWait, leading, trailing, equalityFn
 *
 */
export default useDebounce;
